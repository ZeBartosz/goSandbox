# Prior Go and parser foundation

The user already has working Go project experience from LunaSQL and go-to-php-compiler: structs, slices, maps, pointers, file-backed JSON persistence, lexing, parsing, ASTs, and simple execution/code generation are not first-time topics. Future sessions should avoid re-teaching Go basics and should focus the challenge on byte-level storage design and correctness.
