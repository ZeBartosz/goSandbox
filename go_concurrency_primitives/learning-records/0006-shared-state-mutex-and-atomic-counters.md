# Shared state, mutexes, and atomic counters

The learner completed race-safe mutex and atomic counter exercises. They understood that a mutex protects only the critical section, not all goroutines, and that concurrency is not automatically faster when many goroutines contend for the same shared value.
